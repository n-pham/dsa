[project]
name = "dsa"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]

[tool.ruff.lint.per-file-ignores]
"*.py" = ["F401"]

[tool.uv]
python-preference = "managed"
